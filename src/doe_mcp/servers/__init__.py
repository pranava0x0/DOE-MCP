"""Per-domain stdio entry points (decision 0003-B).

One pip package, several servers. `doe-mcp configure` registers only the ones
a user picks, so a literature-only install never carries the science domains'
dependency union.
"""
